plugins: ['kramdown']
exclude: ['config.ru', 'Gemfile', 'Gemfile.lock', 'vendor', 'Procfile', 'Rakefile']
page_gen:
  - data: places
    template: site
    filename: slug
    dir: locations
    extension: html
